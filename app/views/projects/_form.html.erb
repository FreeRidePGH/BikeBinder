<%= form_for(@project) do |f| %>
<%= render 'shared/error_messages', :object => f.object %>

<div id="field">
<%= f.label :bike_number %>
<%= text_field_tag :bike_id %>
</div>

<div class="actions">
<%= f.submit "Submit" %>
</div>

<% end %>
