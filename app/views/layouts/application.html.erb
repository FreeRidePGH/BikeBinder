<!DOCTYPE html>
<html>
<head>
  <title>BikeBinder</title>
  <%= stylesheet_link_tag    "application" %>
  <!-- lets get Bootstrapping -->
  <%= stylesheet_link_tag    "bootstrap" %>


  <%= javascript_include_tag "application" %>


  <%= csrf_meta_tags %>
  <%= favicon_link_tag '/favicon.png' %>
 <!-- need to establish the proper route to serve up the favicon -->
  <!-- <link rel="shortcut icon" type="image/png" href="./favicon.png" /> -->


</head>
<!-- duplicating logic should not be relied upon, just here for a quick fix  -->
<% if header_title.nil? %>
<body class="noTitle">
<% else %>
<body>
<% end %>
  <div class="container">
    <header class="span12" >
      <% if !header_title.nil? %>
      <%= content_tag(:h1, header_title) %>
      <% end %>
      <%= render 'shared/menu' %>
    </header>

    <%= render 'shared/alerts' %>

    <%= yield %>


    <footer class="span8">
      <%= debug(params) if Rails.env.development? %>
    </footer>
  </div>
</body>
</html>
